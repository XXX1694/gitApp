import 'package:flutter/material.dart';

class CoachChooseBlock extends StatelessWidget {
  const CoachChooseBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
