import 'package:flutter/material.dart';

class CoachesBlock extends StatelessWidget {
  const CoachesBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
