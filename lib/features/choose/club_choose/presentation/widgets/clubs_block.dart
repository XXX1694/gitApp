import 'package:flutter/material.dart';

class ClubsBlock extends StatelessWidget {
  const ClubsBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
